{% extends 'layout/autenticado.html.twig' %}
{% block title %}{{ 'piece'|trans }}{% endblock %}
{% block page_title %}{{ 'pieces' | trans }}{% endblock page_title %}
{% block body %}
    <div class="m-portlet">
        <div class="m-portlet__head">
            <div class="m-portlet__head-caption">
                <div class="m-portlet__head-title">
                    <span class="m-portlet__head-icon"><i class="flaticon-list-3"></i></span>
                    <h3 class="m-portlet__head-text">
                        {{ 'list' | trans }}
                    </h3>
                </div>
            </div>
            <div class="m-portlet__head-tools">
                <ul class="m-portlet__nav">
                    <li class="m-portlet__nav-item">
                        <a class="btn btn-sm btn-default" data-href="{{ path('pieza_index') }}" id="refrescar">
                            <i class="flaticon-refresh"></i>{{ 'refresh' |trans }}
                        </a>
                    </li>
                    <li class="m-portlet__nav-item">
                        <a class="btn btn-sm btn-primary edicion active" data-href="{{ path('pieza_new') }}"
                           data-toggle="m-tooltip" data-placement="bottom" title="" data-original-title="{{ 'piece_register_tooltip' | trans }}">
                            <i class="flaticon-add"></i> {{ 'register' |trans }}
                        </a>
                    </li>
                </ul>
            </div>
        </div>
        <div class="m-portlet__body">
            {% include 'pieza/_table.html.twig' %}
        </div>
    </div>
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    {% include 'pieza/script.js.twig' %}
    <script>
        var total={{ piezas |length  }}
        pieza.init();
    </script>
{% endblock %}