{% extends 'layout/autenticado.html.twig' %}
{% block title %}{{ 'laboratory' | trans}}{% endblock %}
{% block page_title %}{{ 'laboratories' | trans }}{% endblock page_title %}
{% block body %}
    <div class="m-portlet">
        <div class="m-portlet__head">
            <div class="m-portlet__head-caption">
                <div class="m-portlet__head-title">
                    <span class="m-portlet__head-icon"><i class="flaticon-list-3"></i></span>
                    <h3 class="m-portlet__head-text">
                        {{ 'list' | trans }}
                    </h3>
                </div>
            </div>
            <div class="m-portlet__head-tools">
                <ul class="m-portlet__nav">
                    <li class="m-portlet__nav-item">
                        <a class="btn btn-sm btn-default text-uppercase" data-href="{{ path('laboratorio_index') }}" id="refrescar">
                            <i class="flaticon-refresh"></i>{{ 'refresh' |trans }}
                        </a>
                    </li>
                    {% if is_granted('ROLE_JEFETECNICOINSTITUCIONAL') %}
                    <li class="m-portlet__nav-item">
                        <a class="btn btn-sm btn-primary text-uppercase edicion active" data-href="{{ path('laboratorio_new') }}"
                           data-toggle="m-tooltip" data-placement="bottom" title="" data-original-title="{{ 'laboratory_register_tooltip' | trans }}">
                            <i class="flaticon-add"></i> {{ 'register' |trans }}
                        </a>
                    </li>
                    {% endif %}
                </ul>
            </div>
        </div>
        <div class="m-portlet__body">
            {% include 'laboratorio/_table.html.twig' %}
        </div>
    </div>
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    {% include 'laboratorio/script.js.twig' %}
    <script>
        var total={{ laboratorios |length  }}
            laboratorio.init();
    </script>
{% endblock %}